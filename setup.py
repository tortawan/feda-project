from setuptools import setup, find_packages

setup(
    name='feda_project',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        # Add dependencies here, or leave blank if using requirements.txt
    ],
    author='Tawan Teopipithaporn',
    description='Forest-guided Estimation of Distribution Algorithm (FEDA) project',
)
